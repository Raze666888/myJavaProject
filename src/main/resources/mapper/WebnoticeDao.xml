<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javaPro.myProject.modules.webnotice.dao.WebnoticeDao">

    <resultMap type="com.javaPro.myProject.modules.webnotice.entity.Webnotice" id="WebnoticeMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="type" column="type" jdbcType="INTEGER"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="img" column="img" jdbcType="VARCHAR"/>
        <result property="typeName" column="type_name" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="WebnoticeMap">
        select w.id,
               w.title,
               w.type,
               w.content,
               w.create_time,
               w.update_time,
               w.status,
               w.img,
               t.type_name
        from webnotice w
        left join t_type t on t.id = w.type
        where w.id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="WebnoticeMap">
        select
        w.id, w.title, w.type, w.content, w.create_time, w.update_time, w.status, w.img,
        t.type_name
        from webnotice w
        left join t_type t on t.id = w.type
        <where>
            <if test="id != null">
                and w.id = #{id}
            </if>
            <if test="title != null and title != ''">
                and w.title like concat('%',#{title},'%')
            </if>
            <if test="type != null and type != ''">
                and w.type = #{type}
            </if>
            <if test="content != null and content != ''">
                and w.content = #{content}
            </if>
            <if test="createTime != null">
                and w.create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and w.update_time = #{updateTime}
            </if>
            <if test="status != null and status != ''">
                and w.status = #{status}
            </if>
            <if test="img != null and img != ''">
                and w.img = #{img}
            </if>
        </where>
order by w.create_time desc
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from webnotice
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="type != null and type != ''">
                and type = #{type}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="img != null and img != ''">
                and img = #{img}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into webnotice(title, type, content, create_time, update_time, status, img)
        values (#{title}, #{type}, #{content}, now(),now(), #{status}, #{img})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into webnotice(title, type, content, create_time, update_time, status, img)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title}, #{entity.type}, #{entity.content}, #{entity.createTime}, #{entity.updateTime},
            #{entity.status}, #{entity.img})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into webnotice(title, type, content, create_time, update_time, status, img)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title}, #{entity.type}, #{entity.content}, #{entity.createTime}, #{entity.updateTime},
            #{entity.status}, #{entity.img})
        </foreach>
        on duplicate key update
        title = values(title),
        type = values(type),
        content = values(content),
        create_time = values(create_time),
        update_time = values(update_time),
        status = values(status),
        img = values(img)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update webnotice
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="type != null and type != ''">
                type = #{type},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>

                update_time = now(),

            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="img != null and img != ''">
                img = #{img},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from webnotice
        where id = #{id}
    </delete>

</mapper>

