<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javaPro.myProject.modules.message.dao.MessageDao">

    <resultMap type="com.javaPro.myProject.modules.message.entity.Message" id="MessageMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="createtime" column="createtime" jdbcType="TIMESTAMP"/>
        <result property="updatetime" column="updatetime" jdbcType="TIMESTAMP"/>
        <result property="senderid" column="senderid" jdbcType="INTEGER"/>
        <result property="receiveid" column="receiveid" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="senderName" column="senderName" jdbcType="VARCHAR"/>
        <result property="receiverName" column="receiverName" jdbcType="VARCHAR"/>
        <result property="senderRole" column="senderRole" jdbcType="VARCHAR"/>
        <result property="receiverRole" column="receiverRole" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="MessageMap">
        select
          id, content, createtime, updatetime, senderid, receiveid, status
        from message
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MessageMap">
        select
        m.id, content, m.createtime, updatetime, senderid, receiveid, status,
               s.username as senderName,r.username as receiverName
        from message m
        left join sysuser s on s.id = m.senderid
        left join sysuser r on r.id = m.receiveid
        <where>
            <if test="id != null">
                and m.id = #{id}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="createtime != null">
                and m.createtime = #{createtime}
            </if>
            <if test="updatetime != null">
                and updatetime = #{updatetime}
            </if>
            <if test="senderid != null">
                and senderid = #{senderid}
            </if>
            <if test="receiveid != null">
                and receiveid = #{receiveid}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
order by createtime desc
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from message
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="createtime != null">
                and createtime = #{createtime}
            </if>
            <if test="updatetime != null">
                and updatetime = #{updatetime}
            </if>
            <if test="senderid != null">
                and senderid = #{senderid}
            </if>
            <if test="receiveid != null">
                and receiveid = #{receiveid}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into message(content, createtime, updatetime, senderid, receiveid, status)
        values (#{content}, now(), now(), #{senderid}, #{receiveid}, #{status})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into message(content, createtime, updatetime, senderid, receiveid, status)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.content}, #{entity.createtime}, #{entity.updatetime}, #{entity.senderid}, #{entity.receiveid}, #{entity.status})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into message(content, createtime, updatetime, senderid, receiveid, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.content}, #{entity.createtime}, #{entity.updatetime}, #{entity.senderid}, #{entity.receiveid}, #{entity.status})
        </foreach>
        on duplicate key update
        content = values(content),
        createtime = values(createtime),
        updatetime = values(updatetime),
        senderid = values(senderid),
        receiveid = values(receiveid),
        status = values(status)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update message
        <set>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="createtime != null">
                createtime = #{createtime},
            </if>
                updatetime = now(),
            <if test="senderid != null">
                senderid = #{senderid},
            </if>
            <if test="receiveid != null">
                receiveid = #{receiveid},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from message where id = #{id}
    </delete>

    <!--获取聊天记录-->
    <select id="getChatMessages" resultMap="MessageMap">
        select
            m.id, content, m.createtime, updatetime, senderid, receiveid, status,
            s.username as senderName, s.role as senderRole,
            r.username as receiverName, r.role as receiverRole
        from message m
        left join sysuser s on s.id = m.senderid
        left join sysuser r on r.id = m.receiveid
        where
            (m.senderid = #{userId} and m.receiveid = #{partnerId})
            or
            (m.senderid = #{partnerId} and m.receiveid = #{userId})
        order by m.createtime asc
    </select>

</mapper>

