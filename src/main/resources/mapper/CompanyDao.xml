<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javaPro.myProject.modules.company.dao.CompanyDao">

    <resultMap type="com.javaPro.myProject.modules.company.entity.Company" id="CompanyMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="phonenumber" column="phonenumber" jdbcType="VARCHAR"/>
        <result property="address" column="address" jdbcType="VARCHAR"/>
        <result property="zhuceshijian" column="zhuceshijian" jdbcType="VARCHAR"/>
        <result property="createtime" column="createtime" jdbcType="TIMESTAMP"/>
        <result property="updatetime" column="updatetime" jdbcType="TIMESTAMP"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="createid" column="createid" jdbcType="INTEGER"/>
        <result property="serviceTime" column="service_time" jdbcType="VARCHAR"/>
        <result property="avgRating" column="avg_rating" jdbcType="DOUBLE"/>
        <result property="ratingCount" column="rating_count" jdbcType="INTEGER"/>
        <result property="serviceArea" column="service_area" jdbcType="VARCHAR"/>
        <result property="longitude" column="longitude" jdbcType="DOUBLE"/>
        <result property="latitude" column="latitude" jdbcType="DOUBLE"/>
        <result property="province" column="province" jdbcType="VARCHAR"/>
        <result property="city" column="city" jdbcType="VARCHAR"/>
        <result property="district" column="district" jdbcType="VARCHAR"/>

    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="CompanyMap">
        select
          id, phonenumber, address, zhuceshijian, createtime, updatetime, status, createid,
          service_time, avg_rating, rating_count, service_area, longitude, latitude, province, city, district
        from company
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="CompanyMap">
        select
          id, phonenumber, address, zhuceshijian, createtime, updatetime, status, createid,
          service_time, avg_rating, rating_count, service_area, longitude, latitude, province, city, district
        from company
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="phonenumber != null and phonenumber != ''">
                and phonenumber = #{phonenumber}
            </if>
            <if test="address != null and address != ''">
                and address = #{address}
            </if>
            <if test="zhuceshijian != null and zhuceshijian != ''">
                and zhuceshijian = #{zhuceshijian}
            </if>
            <if test="createtime != null">
                and createtime = #{createtime}
            </if>
            <if test="updatetime != null">
                and updatetime = #{updatetime}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="createid != null">
                and createid = #{createid}
            </if>
        </where>
order by createtime desc
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from company
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="phonenumber != null and phonenumber != ''">
                and phonenumber = #{phonenumber}
            </if>
            <if test="address != null and address != ''">
                and address = #{address}
            </if>
            <if test="zhuceshijian != null and zhuceshijian != ''">
                and zhuceshijian = #{zhuceshijian}
            </if>
            <if test="createtime != null">
                and createtime = #{createtime}
            </if>
            <if test="updatetime != null">
                and updatetime = #{updatetime}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="createid != null">
                and createid = #{createid}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into company(phonenumber, address, zhuceshijian, createtime, updatetime, status, createid, province, city, district, longitude, latitude)
        values (#{phonenumber}, #{address}, #{zhuceshijian}, now(), now(), #{status}, #{createid}, #{province}, #{city}, #{district}, #{longitude}, #{latitude})
    </insert>

    <!--新增指定ID的记录-->
    <insert id="insertWithId">
        insert into company(id, phonenumber, address, zhuceshijian, createtime, updatetime, status, createid, province, city, district, longitude, latitude)
        values (#{id}, #{phonenumber}, #{address}, #{zhuceshijian}, now(), now(), #{status}, #{createid}, #{province}, #{city}, #{district}, #{longitude}, #{latitude})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into company(phonenumber, address, zhuceshijian, createtime, updatetime, status, createid)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.phonenumber}, #{entity.address}, #{entity.zhuceshijian}, #{entity.createtime}, #{entity.updatetime}, #{entity.status}, #{entity.createid})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into company(phonenumber, address, zhuceshijian, createtime, updatetime, status, createid)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.phonenumber}, #{entity.address}, #{entity.zhuceshijian}, #{entity.createtime}, #{entity.updatetime}, #{entity.status}, #{entity.createid})
        </foreach>
        on duplicate key update
        phonenumber = values(phonenumber),
        address = values(address),
        zhuceshijian = values(zhuceshijian),
        createtime = values(createtime),
        updatetime = values(updatetime),
        status = values(status),
        createid = values(createid)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update company
        <set>
            <if test="phonenumber != null and phonenumber != ''">
                phonenumber = #{phonenumber},
            </if>
            <if test="address != null and address != ''">
                address = #{address},
            </if>
            <if test="zhuceshijian != null and zhuceshijian != ''">
                zhuceshijian = #{zhuceshijian},
            </if>
            <if test="createtime != null">
                createtime = #{createtime},
            </if>

                updatetime = now(),

            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="createid != null">
                createid = #{createid},
            </if>
            <if test="serviceTime != null">
                service_time = #{serviceTime},
            </if>
            <if test="avgRating != null">
                avg_rating = #{avgRating},
            </if>
            <if test="ratingCount != null">
                rating_count = #{ratingCount},
            </if>
            <if test="serviceArea != null">
                service_area = #{serviceArea},
            </if>
            <if test="province != null and province != ''">
                province = #{province},
            </if>
            <if test="city != null and city != ''">
                city = #{city},
            </if>
            <if test="district != null and district != ''">
                district = #{district},
            </if>
            <if test="longitude != null">
                longitude = #{longitude},
            </if>
            <if test="latitude != null">
                latitude = #{latitude},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from company where id = #{id}
    </delete>

    <!--根据创建者ID查询服务商信息-->
    <select id="queryByCreateId" resultMap="CompanyMap">
        select
          id, phonenumber, address, zhuceshijian, createtime, updatetime, status, createid,
          service_time, avg_rating, rating_count, service_area, longitude, latitude
        from company
        where createid = #{createid}
        limit 1
    </select>

</mapper>

